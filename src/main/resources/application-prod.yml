spring:
  datasource:
    url: jdbc:postgresql://localhost:3306/myopenrestaurant
    username: root
    password: admin
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: false
  liquibase:
    change-log: classpath:changelog/main.xml